// Colors

pub struct Rgba8Uint {
    r: u8,
    g: u8,
    b: u8,
    a: u8,
}

pub struct Rgba64Float {
    r: f64,
    g: f64,
    b: f64,
    a: f64,
}

// size
pub struct Size {
    pub width: u32,
    pub height: u32,
}